
## **Installation Instructions**

### **1. Complete Setup Commands**
```bash
# Create and navigate to project root
mkdir cognitive-insight-engine
cd cognitive-insight-engine

# Create directory structure
mkdir -p public/{core,models}

# Copy your existing files to the public folder
# (Assuming your current files are in a folder called 'mind-engine')
cp -r ../mind-engine/* public/

# Create package.json
cat > package.json << 'EOF'
{
  "name": "cognitive-insight-engine",
  "version": "3.0.0",
  "description": "Advanced psychological pattern analysis system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test.js"
  },
  "keywords": ["psychology", "cognitive", "nlp", "analysis"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
EOF

# Create server.js
cat > server.js << 'EOF'
const express = require('express');
const path = require('path');
const app = express();
const PORT = 3000;

app.use(express.static('public'));

app.get('/', (req, res) => {
  res.sendFile(path.join(__dirname, 'public', 'index.html'));
});

app.listen(PORT, () => {
  console.log(`Server running at http://localhost:${PORT}`);
});
EOF

# Create .gitignore
echo "node_modules/" > .gitignore

# Install dependencies
npm install

# Start the server
npm start
